import { PipeCollectionPipe } from './pipe-collection.pipe';

describe('PipeCollectionPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeCollectionPipe();
    expect(pipe).toBeTruthy();
  });
});
